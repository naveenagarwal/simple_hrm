<%= form_for(@user_management_user) do |f| %>
  <% if @user_management_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_management_user.errors.count, "error") %> prohibited this user_management_user from being saved:</h2>

      <ul>
      <% @user_management_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
