$("#modal-data-container").html('<%= j(render partial: "modal_form", locals: { action: "Editing" }) %>');

$('#document-modal').modal({ keyboard: false });   // initialized with no keyboard
$('#document-modal').modal('show');                // initializes and invokes show immediately


