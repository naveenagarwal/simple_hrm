<%= form_for(@qualifications_skill) do |f| %>

  <%= render partial: "/shared/form_errors", locals: { obj: @qualifications_skill, name: "qualifications skill" } %>

  <fieldset>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </fieldset>
  <fieldset>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </fieldset>

  <fieldset>
    <div class="actions">
      <%= f.submit nil, class: "btn btn-primary" %>
    </div>
  </fieldset>

<% end %>
