$("#modal-data-container").html('<%= j(render partial: "modal_form", locals: { action: "New" }) %>');

$('#subscriber-modal').modal({ keyboard: false });   // initialized with no keyboard
$('#subscriber-modal').modal('show');