<%= form_for([:job, @job_title], multipart: true) do |f| %>
  <% if @job_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_title.errors.count, "error") %> prohibited this job_title from being saved:</h2>

      <ul>
      <% @job_title.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </fieldset>
  <fieldset>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </fieldset>
  <fieldset>
    <%= f.label :specification, specification_file_name(@job_title)  %><br>
    <%= f.file_field :specification %>
  </fieldset>
  <fieldset>
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </fieldset>

  <fieldset>
    <div class="actions">
      <%= f.submit nil, class: "btn btn-primary" %>
    </div>
  </fieldset>

<% end %>
