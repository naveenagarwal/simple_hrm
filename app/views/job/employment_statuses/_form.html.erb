<%= form_for(@job_employment_status) do |f| %>
  <% if @job_employment_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_employment_status.errors.count, "error") %> prohibited this job_employment_status from being saved:</h2>

      <ul>
      <% @job_employment_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
