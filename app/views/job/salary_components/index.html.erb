<h1>Listing job_salary_components</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @job_salary_components.each do |job_salary_component| %>
      <tr>
        <td><%= link_to 'Show', job_salary_component %></td>
        <td><%= link_to 'Edit', edit_job_salary_component_path(job_salary_component) %></td>
        <td><%= link_to 'Destroy', job_salary_component, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Salary component', new_job_salary_component_path %>
